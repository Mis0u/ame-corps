<div id="carouselExampleIndicators" class="carousel slide" data-ride="carousel" data-pause="false">
  <div class="over-text d-flex flex-column align-items-center justify-content-center">
    <h1>
      L'âme Body
    </h1>
    <p>Offrez-vous un moment de relaxation</p>
  </div>
  <ol class="carousel-indicators">
  {% for i in 1..total_images %}
    <li data-target="#carouselExampleIndicators" data-slide-to="{{i - 1}}" class="{% if loop.first	%}active{% endif %}"></li>
  {% endfor %}
  </ol>
  <div class="carousel-inner">
  {% for picture in caroussel %}
     <div class="carousel-item {% if loop.first	%}active{% endif %}">
      <img class="d-block w-100" src="{{ vich_uploader_asset(picture, 'imageFile') }}" alt="First slide">
    </div>
  {% endfor %}
  </div>
  <a class="carousel-control-prev" href="#carouselExampleIndicators" role="button" data-slide="prev">
    <span class="carousel-control-prev-icon" aria-hidden="true"></span>
    <span class="sr-only">Previous</span>
  </a>
  <a class="carousel-control-next" href="#carouselExampleIndicators" role="button" data-slide="next">
    <span class="carousel-control-next-icon" aria-hidden="true"></span>
    <span class="sr-only">Next</span>
  </a>
</div>